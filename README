Have a look at mod3.txt to get an idea what the layout looks like. saneo is the xbd file you should use, but saneo.xmodmap generally works too if you must use a different machine. neo.map is a slightly outdated version for pure terminals.
